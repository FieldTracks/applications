services:
  - docker

script:
  - make

before_deploy:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

deploy:
  provider: script
  script: bash docker push fieldtracks:latest
  on:
    branch: master